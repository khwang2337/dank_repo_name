#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main () {
	
	/*
    struct song_node *table[26];
    int i;
    for (i = 0; i < 26; i++) {
        table[i] = 0;
    }
    insert_front(table[0],"Hello","Adele");
    //print_list(table[0]);
    //printf("%p %s",table[0],(*table[0]).song);
    //print_list(table[0]);
    printf("is this working\n\n");
    */
    
    return 0;
    
}